name: Secrets Management Demo

on:
  workflow_dispatch:  # Manuel seulement pour la sécurité

jobs:
  secret-handling:
    runs-on: ubuntu-latest
    
    # Définir un environnement (optionnel mais recommandé)
    environment: testing
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3